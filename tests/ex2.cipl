float compute_w(
    int list_elem, 
    int elem,
    int list h
) {
    return (elem * 4.3) / 2;
}

int main() {
    int list l;
    
    int n;
    read(n);

    int i;
    int elem;
    for (i = 0; i < n; i = i + 1) {
        read(elem);
        l = elem : l;
    }

    float list w;
    float list it;
    w = compute_w >> l;
    i = 0;

    for (it = w; it != NIL; it = !it) {
        write("w_(");
        write(i);
        write("): ");
        writeln(?it);
        i = i + 1;
    }

    it = %it;

    return 0;
}