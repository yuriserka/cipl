float compute_w(
    // TODO: nao permitir casar: <type, int list> <id, _elem>
    int list_elem, 
    int elem,
    int list h
) {
    return (elem * 4.3) / 2;
}

int main() {
    int list l;
    
    int n;
    read(n);

    int i, elem;
    for (i = 0; i < n; ++i) {
        read(elem);
        l = elem : l;
    }

    float list w, it;
    w = compute_w >> l;
    i = 0;

    for (it = w; it != NIL; it = !it) {
        write("w_(");
        write(i);
        write("): ");
        writeln(?it);
        ++i;
    }

    it = %it;

    return 0;
}